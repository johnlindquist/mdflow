---
description: run the test suite and fix what fails
---

Run this project's test suite. If anything fails, find the root cause and fix
it — the code, not the test, unless the test is genuinely wrong. Show me the
failing output first, then the fix.
